<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CorrectButtonStyle" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">#000000</item> <!-- Colore del testo (nero) -->
        <item name="android:background">@drawable/correct_button_background</item> <!-- Sfondo trasparente con bordo verde -->
    </style>

    <style name="WrongButtonStyle" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">#FFFFFF</item> <!-- Colore del testo (bianco) -->
        <item name="android:background">@drawable/wrong_button_background</item> <!-- Sfondo trasparente con bordo rosso -->
    </style>
</resources>

